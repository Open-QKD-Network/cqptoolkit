Using sublime with MSBuild
=============================
One way to get msbuild working with sublime is to make sure the compiler is in the PATH (running codeblocks from a developer command prompt will do).

Using MSYS2
===========
Install the dependencies in the MSYS2 shell by running installMSYS2Dependencies.bat